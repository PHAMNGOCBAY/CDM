
- Do not use emojis in your responses.
- Always copy output files (such as generated markdown summaries or images) from the temporary scratch/artifact memory (`<appDataDir>\brain\<conversation-id>`) into the project directory (e.g., `docs/`, `images/`, or `data/`) so that they are persisted inside the workspace.
