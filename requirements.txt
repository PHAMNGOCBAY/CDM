python-docx==1.2.0
streamlit>=1.35.0
numpy>=2.0,<3.0
pandas>=2.2.2          # pandas <2.2.2 pin numpy<2 → conflict
plotly>=6.0
matplotlib>=3.9        # matplotlib <3.9 pin numpy<2 → conflict
openpyxl>=3.1
xlrd>=2.0
jinja2>=3.1
sympy>=1.14
pydeck>=0.9
reportlab>=4.0
fpdf2>=2.8
ezdxf>=1.4
unidecode>=1.4
shapely>=2.1
sqlalchemy>=2.0
geojson>=3.2
scipy>=1.16            # scipy <1.16 pin numpy<2.3
# Solver Winkler p-y: dùng scripts/winkler_np.py (NumPy thuần) — KHÔNG cần PyNite/anastruct
# Lý do bỏ: PyNiteFEA + anastruct fail build trên Streamlit Cloud Python 3.14
# (numpy/scipy chưa có wheel cho 3.14). winkler_np.py chạy mọi Python.
geotech-staff-engineer>=4.6,<5.0   # slope_stability; đòi numpy>=2.0 nên giữ numpy 2.x
# openseespy/gmsh/geopandas: chỉ local (cần binary C++/GDAL) — KHÔNG đưa lên Cloud
